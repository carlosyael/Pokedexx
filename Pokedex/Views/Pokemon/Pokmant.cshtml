@model List<Application.ViewModels.PokemonViewModel>
@{
    ViewData["Title"] = "Mantenimiento Pokemones";
}

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col-3 offset-9">
            <a href="#" class="btn btn-sm btn-outline-success">Crear Nuevo Pokemon</a>
        </div>
    </div>
    <div class="row">
        @if (Model == null || Model.Count == 0)
        {
            <h2>No Hay Pokemones</h2>
        }
        else
        {
            @foreach (Application.ViewModels.PokemonViewModel item in Model)
            {

                <div class="col-4">
                    <div class="card shadow-sm">
                        <img class="bd-placeholder-img card-img-top " src="@item.ImageUrl" />
                        <div class="card-body">
                            <h4>@item.Name</h4>

                            <div clas="d-flex justify-content-between aling-items-center">
                                <div class="btn-group">
                                    <a href="#" class="btn btn-sm btn-outline-info">Editar</a>
                                    <a href="#" class="btn btn-sm btn-outline-danger">Borrar</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }

    </div>
</div>